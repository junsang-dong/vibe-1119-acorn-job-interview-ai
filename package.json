{
  "name": "vibe-1119-acorn-job-interview-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "engines": {
    "node": ">=18"
  }
}
